---
layout: post
title: "14 Décembre"
author: "Pipou"
categories: calendrier
tags: [calendrier]
image: 14_slider.png
---


<!-- 2. The BOX -->
<!-- Clickable box -->
<div id="case-jour"
     style="
       position: relative;
       width: 400px;
       height: 400px;
       padding: 40px;
       border: 5px solid red;
       border-radius: 12px;
       cursor: pointer;
       background-image: url('./assets/img/14.png');
       background-size: cover;
       background-position: center;
       background-repeat: no-repeat;
       color: black;
       font-size: 32px;
       font-weight: bold;
       text-align: center;
       user-select: none;
       transition: all 0.4s ease;
       overflow: hidden;
       margin: 0 auto;
        display: flex;
       align-items: center;
       justify-content: center;
     "
>
  <div id="box-text" style="position: relative; z-index: 2; transition: opacity 0.6s ease;">
  </div>

</div>

<!-- Sparkles -->
 <div id="sparkle-container" style="pointer-events:none; position:fixed; inset:0; overflow:hidden; z-index:9999;"></div>

<style>
.sparkle {
  position: absolute;
  width: 12px;
  height: 12px;
  background: red;
  border-radius: 50%;
  pointer-events: none;
  opacity: 1;
  transform: translate(-50%, -50%) scale(1);
  will-change: transform, opacity;
}
</style>

<script>
function launchSparkles(x, y, count = 200) {
  const container = document.getElementById("sparkle-container");

  for (let i = 0; i < count; i++) {
    const sparkle = document.createElement("div");
    sparkle.className = "sparkle";

    const angle = Math.random() * 2 * Math.PI;
    const speed = 1 + Math.random() * 3;
    const dx = Math.cos(angle) * speed * 30;
    const dy = Math.sin(angle) * speed * 30;

    sparkle.style.left = x + "px";
    sparkle.style.top = y + "px";

    container.appendChild(sparkle);

    requestAnimationFrame(() => {
      sparkle.style.transition = "transform 800ms ease-out, opacity 800ms ease-out";
      sparkle.style.transform = "translate(" + dx + "px, " + dy + "px) scale(0.3)";
      sparkle.style.opacity = "0";
    });

    setTimeout(() => sparkle.remove(), 1500);
  }
}
</script>
<script>
<!-- Typewriter style -->
function typewrite(element, text, speed = 40) {
  element.innerHTML = "";
  let i = 0;

  function typeNext() {
    if (i < text.length) {
      element.innerHTML += text.charAt(i);
      i++;
      setTimeout(typeNext, speed);
    }
  }

  typeNext();
}
</script>

<!-- Revealed content outside the box -->
<div id="revealed-content"
     style="display: none; margin: 20px auto; width: 80%; text-align: center; font-size: 18px; color: black;">
  <h2>Le jeu du jour consiste à deviner le prix d'objets aléatoires.</h2>
  <p>Ci-dessous se trouve une liste d'objets dont tu dois deviner le prix. Oui, cela n'a aucun sens et ces objets ont été choisis aléatoirement. Une fois tes choix arrêtés, rends-toi au bas de cette page pour découvrir les réponses.</p>
</div>

<div id="revealed-img"
     style="display: none; margin: 20px auto; width: 80%; text-align: center; font-size: 18px; color: black;">
  <img src="{{ site.github.url }}/assets/img/jeu14_1sans.png" width="800" alt="Image 1">
  <img src="{{ site.github.url }}/assets/img/jeu14_2sans.png" width="800" alt="Image 2">
  <img src="{{ site.github.url }}/assets/img/jeu14_3sans.png" width="800" alt="Image 3">
  <img src="{{ site.github.url }}/assets/img/jeu14_4sans.png" width="800" alt="Image 4">
</div>


<script>
document.addEventListener("DOMContentLoaded", () => {
  const box = document.getElementById("case-jour");
  const boxText = document.getElementById("box-text");
  const revealed = document.getElementById("revealed-content");
  const revealedimg = document.getElementById("revealed-img");

  box.addEventListener("click", (e) => {
    // Launch sparkles
    launchSparkles(e.clientX, e.clientY, 25);
    
    if (box.dataset.opened === "true") return;
    box.dataset.opened = "true";

    // Change text inside box
    boxText.style.opacity = "0";
    setTimeout(() => {
      boxText.innerHTML = "🎁 Le juste prix";
      boxText.style.opacity = "1";
      box.style.backgroundImage = "none";
    }, 400);

    // Show content below box typed-style
    revealed.style.display = "block";
    typewrite(revealed, revealed.textContent.trim(), 5); // 5ms per letter
    revealedimg.style.display = "block";
  });
});
</script>


[Les réponses par ici !]({{ "/14decanswers" | relative_url }})
